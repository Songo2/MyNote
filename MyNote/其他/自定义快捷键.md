`ctrl`+`num1` 插入代码块
`crtl`+`num2` 切换列表和任务
`ctrl`+`num3` 插入数学块
`ctrl`+`num4` 插入模板
`ctrl`+`num5` 插入表格
`ctrl`+`num6` 全局搜索并替换 (插件Global Search And Replace)

`ctrl`+`←` 左对齐
`ctrl`+`→` 右对齐
`crtl`+`↓` 居中对齐
`crtl`+`+` 放大
`crtl`+`-`缩小

`ctrl`+`H` 本文件查找并替换



